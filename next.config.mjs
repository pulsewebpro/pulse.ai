/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
  // No additional configuration is needed; defaults suffice for this simple site.
};

export default nextConfig;